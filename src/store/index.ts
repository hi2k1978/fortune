import { createStore } from "vuex";

const rootState = {};
export type RootState = typeof rootState;

export default createStore({
  state: rootState,
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
